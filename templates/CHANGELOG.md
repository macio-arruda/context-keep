# Changelog (trail layer)

> What changed and when. Append-only, terse, rarely read in full.
> One line per change, with a pointer to the canonical file. No narrative:
> the "why" lives in DECISIONS.md, not here. Roll to an archive when this passes
> the cap, so the hot path never carries the full history.

## <YYYY-MM-DD>

- <type>: <what changed in one line> -> `<file>` (<id>)

<!-- Types: Added, Changed, Fixed, Removed. If an entry needs a paragraph, the
     paragraph belongs in DECISIONS.md and this line points to it. -->
